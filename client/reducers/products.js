const products = (state = [], action) => {
    switch (action.type) {
        case 'Fetch_Products':
            return state;
        default:
            return state;
    }
}

export default products;
